import React from "react";
import Routing from "./Routing";

function App() {
  // function get() {
  //   return <Create post={postData} />;
  // }
  // function trigerComponent(e) {}
  return (
    <div className="container mt-5 mb-3">
      <Routing />
    </div>
  ); //<Create post={postData} />
}

export default App;
